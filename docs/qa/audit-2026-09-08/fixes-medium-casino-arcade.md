# Casino and Arcade medium fixes — 2026-09-08

Revalidated all nine assigned medium findings against the completed P1 source. Eight retain confirmed gaps addressed in this pass; CA-12 is already fixed by P1 ARC-02 and is verified again rather than counted as a new implementation. CA-09's pending grid lock is also already present from P1, but its submitted identity/result labeling still needed completion. No transactions were submitted for this subtask.

| Finding | Revalidation and final change | Focused evidence |
| --- | --- | --- |
| CA-04: wallet approval called submitted | Confirmed: Roulette's close guard used submitted text before a proof existed. The dialog consumes the shared normalized transaction phase; preparation/approval says to confirm or reject in the wallet, and submitted copy requires the shared proof-aware phase. Preparation immediately locks the wager UI. | Actual CasinoDialog test injects buildingTransaction, checks wallet guidance/close guard, then provides transaction hash and checks submitted close guard. Shared phase normalization itself is covered by transaction-core tests; this component runner isolates that adapter. |
| CA-06: small mobile combinations and 151 Tab stops | Confirmed: pointer boundary geometry remains intentionally narrow. Added a native Bet type select and >=44px canonical combination rows on mobile, plus the same optional list on desktop/tablet. Split/street/corner/six-line use the existing canonical combination source. The original board keeps its number-center/edge pointer contract and now has one Tab stop with arrow/Home/End navigation. | Actual component selects a split and checks selected state; styled primitives tests exercise all four combination categories, touch dimensions, 200% root text size, and original 102 edge targets/37 number centers. |
| CA-07: missing rules, payout and stake distinctions | Confirmed. Visible game heading is provided by the coordinated shared GameDialogHeading fix. Roulette keeps selected currency visible, adds in-dialog rules, reads every payout multiplier from the contract, displays selection odds, states minimum per selection versus maximum total stake, and labels maximum return as including stake. Settled results show exact gross return and net result from the active round's raw total stake; unavailable denomination/stake stays explicitly unavailable. No offered LEAF limits were relaxed. | Actual dialog shows 17:1 split profit plus returned stake; raw 1,234,567-unit USDC result still resolves correctly during recovery. Live read-only multicall returned all 13 multipliers: 35,17,11,8,5,2,2,1,1,1,1,1,1. Source evidence/limits below. |
| CA-08: unreadable Spin result and unsigned losses | Confirmed: missing logs could pick a random wheel index and discard visible recovery; negative toast amounts lost their minus. Spin completion is now resolved/unavailable, never random. It decodes only matching contract/player/plant events, fetches the canonical receipt when wallet logs are absent, and saves a public account/plant-scoped receipt identity before that fetch. An unavailable panel survives reopen, offers read-only retry and receipt link (Activity guidance if no hash); retry does not clear a newer paid pending round. Confirmed-round cleanup is synchronous; late canonical receipt callbacks are scoped to the original commitment and cannot clear a new secret or animate a new round. Receipt cache deletion checks the expected transaction hash. A shared signed formatter supplies toast and persistent PTS/lifetime text; zero/negative-only results use neutral/warning feedback. | Actual SpinGameTransaction checks missing logs plus failed canonical RPC, actual Arcade reopens saved recovery, failed retry retains it, successful retry decodes/removes it, and a negative event toasts “−120 PTS”. Smoke rejects wrong contract/player/plant and missing logs; verifies signed zero/positive/negative, storage identity and older-hash deletion protection. The actual Arcade test starts a new commitment before an older receipt callback returns, then verifies the new reveal key remains intact and the older reward does not replace the current wheel. |
| CA-09: pending Box selection drift | Partly resolved in P1: grid already disabled during wallet/transaction/reconciliation. Completed the missing immutable submitted box/star snapshot and labeled the result with that selection and actual star cost. Grid, game switch and star mode all remain locked through submission. | Actual Arcade: choose Box 3, select star mode with arrow key, begin wallet preparation, assert Box 8 disabled, complete, and assert “Box 3 · 1 star spent”. |
| CA-10: drifting countdown and misleading Ready | Confirmed: Box still decremented callback counts and Spin's footer used metadata-ready as action-ready. Box now stores deadlines from authoritative cooldown seconds, derives remaining time from Date.now, ticks on visibility/focus, and refreshes cooldown reads on resume. Spin uses one derived readiness label for its status, heading and disabled CTA, including cooldown/stars/storage/key/block requirements and transaction progress. | Actual Arcade advances wall time by two minutes while skipping interval callbacks; a 60-second verified cooldown clears immediately on focus. Additional cases confirm loaded metadata still yields disabled “Not enough stars” or cooldown CTAs rather than Ready. P1 stalled-chain/expiry tests still pass unchanged: local elapsed time never authorizes a Spin reveal. |
| CA-11: selection semantics and star control sizing | Confirmed. All Roulette categories expose aria-pressed, including straight/column/dozen/outside; board focus is distinct and roving. Star mode uses the existing tested ToggleGroup radio primitive at its 44px lg size, with arrow-key selection and inert pending protection. Its layout stacks on narrow screens. | Actual Arcade verifies arrow-key star selection and submitted identity. Styled board test verifies a single Tab stop, arrow/Home navigation, Tab exit and selected semantics across categories. |
| CA-12: fallback spendable decimals | Already resolved by P1 ARC-02. Rechecked hooks/useTokenMetadata and all Roulette gates: failed/partial metadata does not provide spendable decimals; fresh wager entry/preparation is blocked; an already-paid reveal remains accessible. New payout-read failure gates are also below the paid recovery branch. | Extended actual recovery test reveals during simultaneous token-config, metadata and payout-rule failures; result stays raw/unknown until its own token metadata recovers. Existing metadata smoke includes legitimate 0/6/18/255 decimals and failed cached refreshes. |
| CA-13: pool refresh overwrites draft | Confirmed: preference initialization still depended on changing pool caps and draft/selection clearing effects. Initialization now runs once per token/decimals identity using the exact minimum; only user edits save preferences. Background pool changes keep partial and full draft strings plus selections. Non-destructive notices explain changed limits; invalid retained selections block submission. The typed preflight propagates a veto instead of sending a newly invalid wager. | Actual CasinoDialog preserves “.” through one pool change and “3” through another, with the current-cap validation message. Retained bets exceeding a changed total/minimum limit show Adjust selected bets rather than disappearing or remaining submittable. |

## Contract/read evidence

- Current deployed payout getter facet was resolved read-only through the diamond's facetAddress selector: `0x1ed693ca42d9568b8a0b12e6da1543b752a1e5de` on Base. `output/inspect-roulette-payout.mjs` uses a single multicall and prints all 13 live values. The frontend uses the same getter and requires all successful validated entries; it has no hardcoded spendable odds fallback.
- Profit-odds meaning is documented in repository `v22/src/libs/LibRouletteStorage.sol:221`; settlement includes principal at `v22/src/libs/LibRoulette.sol:221`. The live payout facet is unverified on Basescan, so this pass does not claim independently verified deployed Roulette settlement bytecode. Exact gross result comes from the actual emitted payout, and net subtracts the actual active-round stake.
- P1 Spin eligibility is unchanged: verified deployed bytecode permits commit+2 through commit+257 inclusive, expires at commit+258; `getSpinRevealState` and preflight simulation remain in place. See the P1 ledger for current implementation address, bytecode hash and disassembly proof. Cosmetic countdown time never substitutes for chain eligibility.

## Validation and limits

- `node scripts/test-arcade-p1.mjs`: actual ArcadeDialog/CasinoDialog and actual SpinGameTransaction, deterministic RPC adapters/stub wallet controls, at 320/390/1440. P1 and medium scenarios pass. This exercises component state/recovery; it does not exercise a real provider submission or pixel fidelity.
- `npx tsx smoke/casino-arcade-p1-smoke.ts`, `npm run spin:smoke`, `npm run casino:smoke`: pass.
- Targeted ESLint on all owned changes and tests: pass. Manually inspected the styled 390px corner picker capture (`output/medium-casino-picker-390.png`); the list has clear separation, full-size controls, and a dark surface supporting its white text.
- Integrated `npx tsc --noEmit --pretty false`: passes after the unrelated in-progress export was restored.
- Styled Roulette: 36 geometry/touch/keyboard/200%-text checks passed across 320-light, 390-dark, 820-light, 1024-dark, 1440-dark and webkit-390-light. Six removal checks then passed after updating the intentional new empty-state wording assertion. All 42 targeted scenarios pass. Initial fixture bootstrap failure was repaired by its owner; no bootstrap checks were weakened. Outputs: `output/medium-casino-playwright` and `output/medium-casino-removal`.
- No package/config/snapshot changes, commits, deployments, or real wallet transactions were made by this subtask. Existing P1 and other agents' uncommitted changes were preserved.
- A confirmed spin without any receipt hash can only guide the player to Activity; it cannot manufacture a canonical receipt lookup. Recovery storage is a last unreadable public result per wallet/plant, not a replacement for full Activity history. If browser storage becomes unavailable, the receipt remains visible for the mounted session.

# Casino and Arcade low fixes — 2026-09-08

Revalidated CA-14, CA-15 and CA-16 against the current high and medium fixes. The medium round had already installed the typed transaction preflight and a single Roulette receipt parser. This round closes the remaining storage race, explains real SpinLeaf outcomes, and removes repeated decisions and presentation ownership. No real wallet transaction or game mutation was sent.

| ID | Before / revalidation | After | Why and limits |
| --- | --- | --- | --- |
| CA-14 | `TransactionPreflight` already propagates false/throw through `GameTransaction` to the transaction kit, but `handleCommitButtonClick` accepted a failed current storage write when an earlier in-memory durable flag was true. A no-op `setItem` also counted as saved. | New Spin commits require a fresh successful write **and exact readback of commitment, secret and commit block**. Failure clears only the current readiness flag, returns false before wallet execution, and exposes **Retry reveal key storage**. Restored storage permits retry. The typed callback is documented as an awaited preflight; the existing shared draft/owner revalidation remains authoritative. | Covers storage disappearing between enabled render and click. An already-paid reveal still uses its retained key and does not require a fresh storage write. Existing pending records are not deleted on failure. Browser storage can still be cleared after submission; this does not claim permanent storage durability. |
| CA-15 | Six validated reward configurations were loaded but hidden behind identical icons. The internal reward-update callback was never invoked. Start/reveal naming and singular star copy differed. | `SpinLeafWheel` displays six numbered decorative positions and an accessible six-row **Possible outcomes** list from the validated contract configuration. Signed PTS, lifetime, LEAF and zero-reward outcomes are explicit. Both reveal defaults and the live footer say **Reveal result**; start copy uses **Start SpinLeaf** and the configured singular/plural star cost. Copy explains that the animation is decorative and timing does not improve the reward. Unknown metadata produces unavailable previews. | Receipt-derived result parsing remains separate from preview configuration. The internal dead callback/state wiring is removed. Its optional public prop is retained with `@deprecated` documentation for source compatibility, as requested by the API-preservation constraint. No odds or reward values are invented. |
| CA-16 | Repeated failure-status sets, Roulette color lists, Box ABI declarations, preference initialization/storage effects, countdown formulas and wheel animation state still had multiple owners. Roulette receipt parsing was already consolidated in `lib/roulette-receipt.ts`. | All game dialogs/wrappers use `isGameTransactionFailure`; pending/syncing states are excluded. `casino-hardening-rules.mjs` owns the red list, re-exported by the public ABI for existing consumers and used by the wheel. Box uses the canonical `BOX_GAME_ABI`. `useCasinoBetPreference` owns once-per-session/token/decimals initialization and persistence only from explicit edits in Roulette, Blackjack and Baccarat. `useArcadeCountdowns` owns one elapsed clock for both Box modes, Spin cooldown and cosmetic reveal delay, wakes on visibility/focus, and stops ticking when idle. `useSpinLeafWheel` owns cosmetic phases, freeze/settle behavior and timer cleanup; `SpinLeafWheel` owns wheel/reward presentation. Arcade reuses the parsed Spin metadata types. Roulette now uses the BB owner's shared `CasinoGameSurface`, preserving its artwork, width, footer and close policy. | Each protocol keeps its own paid-round, receipt, owner and recovery controller. These are cohesive feature extractions, not a generic game state machine. A read-only review caught a mid-spin freeze snap; capturing the computed angle in both DOM and React state fixes reveal/failure stopping and is covered by actual animation tests. Further subdivision of the remaining protocol orchestration is optional maintenance work; no known behavioral gap remains in this assignment. |

## Focused verification

- `node scripts/test-arcade-p1.mjs`: passes actual ArcadeDialog, CasinoDialog and SpinGameTransaction scenarios at **320, 390 and 1440px**. Existing checks retain stalled-chain reveal, off-tab expiry, pending Box snapshots, wallet-stage copy, exact result currency, paid reveal during metadata/config/payout outages, preserved incomplete wager drafts and late older receipt protection. New checks remove storage and make writes throw immediately before a previously enabled Start click, assert zero submitted calls, restore storage through the inline retry, and assert one accepted retry. Six distinct configured rewards, signed/zero outcomes, plural cost and unavailable previews are also checked.
- `tests/frontend/transaction-core-p1.spec.ts --grep TX-03`, 390-light: **12 passed** using the actual shared transaction controller, including false/throw/async preflight veto, current callback and changed-draft validation, duplicate-entry prevention, and late wallet-owner responses.
- `tests/frontend/casino-arcade-low.spec.ts`: **9 passed** across 390-light, 1440-dark and webkit-390-light. Tests compile the actual `app/globals.css` through PostCSS/Tailwind and render the real wheel/hook with real reward formatting. Both reduced and normal motion preserve the current angle on reveal/unavailable stopping; actual paused CSS animation supplies the normal-motion position. The first and last confirmed reward icons geometrically reach the top pointer. The legend stays within the viewport. Shared failure and roulette zero/color decisions also pass.
- `npx tsx smoke/spin-pending-storage-smoke.ts`: passes, including no-op storage and mismatched-secret readback rejection.
- `npx tsx smoke/casino-arcade-p1-smoke.ts` and `node smoke/casino-hardening-smoke.mjs`: pass. Spin eligibility remains **commit+2 through commit+257**, with expiry at **commit+258**; elapsed UI time does not authorize reveal.
- Targeted ESLint and focused TypeScript compilation pass. New production files introduce no `any` or `UntypedValue`; the Spin transaction wrapper also removes its previous receipt/call/status escape casts.
- The BB owner migrated and independently tested shared preference/status consumers and reviewed current storage durability and wheel geometry. Reciprocal read-only review found no descriptor-key, paid-action-gate or shared-shell regression in the BB extraction.

## Visual evidence and scope

Manually inspected the real-style 390-light and 1440-dark screenshots under `output/casino-low-review-final/`: `spinleaf-rewards.png` shows numbered wheel positions, six separated signed reward rows and readable timing guidance. WebKit and normal-motion variants are in the same output directory. These are isolated presentation fixtures with test-only animation controls; they are not screenshots of a live paid round. Component behavior uses mocked wallet/RPC adapters, while the dedicated transaction tests use the actual transaction controller with mocked wallet I/O.

No repository configuration, package, global stylesheet, baseline, full build, server lifecycle, commit or push was changed by this subtask. Test configs and intermediate artifacts are isolated under ignored `output/`. Existing high/medium changes were preserved.

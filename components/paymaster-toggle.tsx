"use client";

interface PaymasterStatusProps {
  className?: string;
  showLabel?: boolean;
}

export function PaymasterStatus(_props: PaymasterStatusProps) {
  void _props;
  return null;
}

interface SponsoredBadgeProps {
  show: boolean;
  className?: string;
}

export function SponsoredBadge({ show, className = "" }: SponsoredBadgeProps) {
  void show;
  void className;
  return null;
}
